<?xml version="1.0" encoding="utf-8"?>
<!-- HIDL/AIDL Device Manifest for Ayaneo Pocket DS (SM8750) -->
<!-- NOTE: HAL versions are best-guess for SM8750 (Android 14).
     Verify against stock: adb shell cat /vendor/etc/vintf/manifest.xml -->
<manifest version="1.0" type="device">

    <!-- Audio -->
    <hal format="hidl">
        <name>android.hardware.audio</name>
        <transport>hwbinder</transport>
        <version>7.1</version>
        <interface>
            <name>IDevicesFactory</name>
            <instance>default</instance>
        </interface>
    </hal>
    <hal format="hidl">
        <name>android.hardware.audio.effect</name>
        <transport>hwbinder</transport>
        <version>7.0</version>
        <interface>
            <name>IEffectsFactory</name>
            <instance>default</instance>
        </interface>
    </hal>

    <!-- Graphics -->
    <hal format="aidl">
        <name>android.hardware.graphics.composer3</name>
        <version>1</version>
        <fqname>IComposer/default</fqname>
    </hal>
    <hal format="aidl">
        <name>android.hardware.graphics.allocator</name>
        <version>2</version>
        <fqname>IAllocator/default</fqname>
    </hal>

    <!-- Bluetooth -->
    <hal format="aidl">
        <name>android.hardware.bluetooth</name>
        <version>1</version>
        <fqname>IBluetoothHci/default</fqname>
    </hal>
    <hal format="hidl">
        <name>android.hardware.bluetooth.audio</name>
        <transport>hwbinder</transport>
        <version>2.1</version>
        <interface>
            <name>IBluetoothAudioProvidersFactory</name>
            <instance>default</instance>
        </interface>
    </hal>

    <!-- DRM -->
    <hal format="aidl">
        <name>android.hardware.drm</name>
        <version>1</version>
        <fqname>IDrmFactory/widevine</fqname>
        <fqname>IDrmFactory/clearkey</fqname>
    </hal>

    <!-- Health -->
    <hal format="aidl">
        <name>android.hardware.health</name>
        <version>1</version>
        <fqname>IHealth/default</fqname>
    </hal>

    <!-- Keymint (replaces keymaster on SM8750) -->
    <hal format="aidl">
        <name>android.hardware.security.keymint</name>
        <version>3</version>
        <fqname>IKeyMintDevice/default</fqname>
    </hal>

    <!-- Power -->
    <hal format="aidl">
        <name>android.hardware.power</name>
        <version>4</version>
        <fqname>IPower/default</fqname>
    </hal>

    <!-- Sensors -->
    <hal format="aidl">
        <name>android.hardware.sensors</name>
        <version>2</version>
        <fqname>ISensors/default</fqname>
    </hal>

    <!-- Thermal -->
    <hal format="aidl">
        <name>android.hardware.thermal</name>
        <version>1</version>
        <fqname>IThermal/default</fqname>
    </hal>

    <!-- USB -->
    <hal format="aidl">
        <name>android.hardware.usb</name>
        <version>1</version>
        <fqname>IUsb/default</fqname>
    </hal>

    <!-- Vibrator -->
    <hal format="aidl">
        <name>android.hardware.vibrator</name>
        <version>2</version>
        <fqname>IVibrator/default</fqname>
    </hal>

    <!-- Wi-Fi -->
    <hal format="hidl">
        <name>android.hardware.wifi</name>
        <transport>hwbinder</transport>
        <version>1.6</version>
        <interface>
            <name>IWifi</name>
            <instance>default</instance>
        </interface>
    </hal>

    <!-- Camera (TODO: verify version from stock) -->
    <hal format="aidl">
        <name>android.hardware.camera.provider</name>
        <version>2</version>
        <fqname>ICameraProvider/internal/0</fqname>
    </hal>

    <!-- Gatekeeper -->
    <hal format="aidl">
        <name>android.hardware.gatekeeper</name>
        <version>1</version>
        <fqname>IGatekeeper/default</fqname>
    </hal>

    <!-- Boot -->
    <hal format="aidl">
        <name>android.hardware.boot</name>
        <version>1</version>
        <fqname>IBootControl/default</fqname>
    </hal>

    <!-- Display (for dual-screen — TODO: verify second instance) -->
    <hal format="hidl">
        <name>android.hardware.light</name>
        <transport>hwbinder</transport>
        <version>2.0</version>
        <interface>
            <name>ILight</name>
            <instance>default</instance>
        </interface>
    </hal>

</manifest>
